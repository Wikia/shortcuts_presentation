package com.fandom.shortcuts.Refactoring;

/**
 * Tip: to jump over method use alt + ( up | down )
 *
 * ctrl + shift + (up | down) to move this method arround to duplicate line press ctrl + d. You can
 * do that with selection also.
 *
 * press ctrl + alt + shift + t and check duplicates in code (this is
 * context aware menu so it's need to be pressed in code, not in comment)
 *
 * press ctrl + alt + shift + t and check move option
 */
public class MovingCode {
  private String thirdMethod() {
    int a;
    int b;
    b = 4;
    a = 2;

    return "";
  }

  private String firstMethod() {
    return "";
  }

  private String secondMethod() {
    return "";
  }
}
